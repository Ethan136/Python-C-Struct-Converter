# C++ Struct Memory Parser Requirements
# =====================================

# Core dependencies
# tkinter is usually included with Python installation
# No external dependencies required for basic functionality

# Development dependencies (optional)
pytest>=6.0.0
pytest-cov>=2.10.0
pytest-xvfb>=3.0.0
black>=21.0.0
flake8>=3.8.0
mypy>=0.800

# Build dependencies
pyinstaller>=5.0.0
auto-py-to-exe>=2.20.0 
jsonschema>=3.2.0 